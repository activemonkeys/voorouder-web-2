// apps/web/lib/utils.ts
// Dit bestand exporteert nu alleen de utilities uit de UI package
// zodat we niet direct alle imports in de hele applicatie hoeven te herschrijven
// (hoewel dat op termijn wel schoner is).
export {cn, slugify} from '@workspace/ui/lib/utils';
